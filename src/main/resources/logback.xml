<?xml version="1.0" encoding="UTF-8"?>
<configuration>
	<appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
		<encoder>
			<!-- Structured key=value pattern for searchable runtime logs -->
			<pattern>ts=%d{yyyy-MM-dd'T'HH:mm:ss.SSSZ} level=%level logger=%logger{36} thread=%thread msg="%msg"%n</pattern>
		</encoder>
	</appender>

	<logger name="com.websystique.springmvc" level="INFO"/>
	<logger name="org.hibernate" level="WARN"/>
	<logger name="org.springframework" level="WARN"/>

	<root level="INFO">
		<appender-ref ref="CONSOLE"/>
	</root>
</configuration>
